apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: test
resources:
  - ../../base
  - ingress.yaml

images:
  - name: us-central1-docker.pkg.dev/my-gcp-project/my-registry/sample-app
    newTag: v1.0.0 # updated by Cloud Deploy promotion, not by CI directly

replicas:
  - name: sample-app
    count: 2
