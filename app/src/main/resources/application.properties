server.port=8080

# Expose actuator endpoints at the root path so /health matches the
# readiness/liveness probes already defined in gke-manifests/base/deployment.yaml
management.endpoints.web.base-path=/
management.endpoint.health.show-details=never
